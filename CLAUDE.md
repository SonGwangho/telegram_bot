@AGENTS.md

# Claude Code 전용 지침

위 공용 규칙(AGENTS.md)이 기준이다. 아래는 **구현 담당자**로서의 추가 지침이다.

## 역할 경계

- 나(Claude)는 **설계·구현·리팩터링·디버깅**을 담당한다.
- **코드 리뷰와 QA 는 Codex 가 담당한다.** 내 코드를 내가 최종 승인하지 않는다.
- 의미 있는 변경을 끝내면 `/review` 로 Codex 리뷰를 돌린다.

## 작업 방식

- 파일 3개 이상 또는 저장 포맷(`data/*.json`)·공개 함수 시그니처를 건드리는 변경은
  **plan mode 로 먼저 계획**을 세우고 승인을 받는다.
- 구현 → 검증(아래) → `/review` → 리뷰 반영 → 커밋. 이 순서를 지킨다.
- 검증은 **문법 검사와 모듈 로딩 확인**으로 한다. 이 프로젝트에는 테스트 스위트가 없고,
  새 테스트 파일·디렉터리를 만들지 않는다(AGENTS.md 개발 원칙).

  ```
  python -m py_compile <수정한 파일들>
  python -c "import main"
  ```

- 봇을 실제로 띄우는 `python main.py` 는 사용자 승인 없이 실행하지 않는다.
  실행하면 텔레그램에 시작 알림이 나가고 폴링이 시작된다.
- 셸은 Windows 다. Git Bash 가 설치돼 있으므로 Bash 도구를 우선 쓴다.
  PowerShell 을 쓸 때는 `&&` 가 아니라 `;` 를 쓰거나 명령을 나눠 실행한다.
- `.env` 와 `data/` 는 읽지 않는다. 실제 토큰·사용자 데이터가 들어 있다.
  설정 키를 알아야 하면 `.env.example` 과 `config.py` 를 본다.
- 기존 파일을 지우거나 대규모로 옮기기 전에 먼저 물어본다.

## Codex 리뷰 결과를 받았을 때

- **BLOCKER / MAJOR**: 반드시 수정한다.
- **MINOR / NIT**: 판단해서 처리하고, 넘긴 항목은 이유를 한 줄로 남긴다.
- 리뷰 지적에 **동의하지 않으면 그대로 수정하지 말고** 근거를 들어 사용자에게 알린다.
  리뷰어는 전체 맥락을 보지 못한 상태로 판단하므로 오탐이 나올 수 있다.
- 리뷰를 반영한 뒤에는 `/review` 를 한 번 더 돌려 회귀를 확인한다.

## 금지

- `~/.codex/` 나 `.claude/settings.json` 을 사용자 요청 없이 수정하지 않는다.
- `git push --force`, `main` 브랜치 직접 푸시 금지.
- 시크릿·토큰·chat_id 를 코드나 로그, 커밋 메시지에 하드코딩하지 않는다.
